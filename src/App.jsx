import { useState } from 'react'
import { ShieldCheck, LogOut } from 'lucide-react'
import { useAuth } from './context/AuthContext'
import Analyzer from './components/Analyzer'
import History from './components/History'

function Login() { const {authenticate} = useAuth(); const [mode,setMode]=useState('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''); const submit=async e=>{e.preventDefault();try{await authenticate(mode,email,password)}catch(e){setError(e.message)}}; return <main className="auth"><section className="auth-card"><ShieldCheck size={42}/><h1>SatyaFin AI</h1><p>Evidence-led investment risk analysis.</p><form onSubmit={submit}><input placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input placeholder="Password (8+ characters)" type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength="8"/>{error&&<small>{error}</small>}<button>{mode==='login'?'Sign in':'Create account'}</button></form><a onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'New here? Create an account':'Already have an account? Sign in'}</a></section></main> }
export default function App(){const {token,logout}=useAuth(); const [tab,setTab]=useState('analyze'); if(!token)return <Login/>; return <main><header><div className="brand"><ShieldCheck/> SatyaFin AI</div><nav><button className={tab==='analyze'?'active':''} onClick={()=>setTab('analyze')}>Analyze</button><button className={tab==='history'?'active':''} onClick={()=>setTab('history')}>Audit history</button><button onClick={logout} title="Sign out"><LogOut size={17}/></button></nav></header><section className="page">{tab==='analyze'?<Analyzer/>:<History/>}</section></main>}
